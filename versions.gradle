rootProject.group = "net.dankito.search"
rootProject.version = "0.6.1-SNAPSHOT"

ext {
    sourceCodeRepositoryBaseUrl = "github.com/dankito/LuceneUtils"
    projectDescription = "Library that facilitates the usage of Lucene, e.g. to index fields, create queries, map search results, additional field types like Boolean and Date field, ..."

    kotlinVersion = "1.3.72"

    luceneVersion = "8.4.1"
    latestLuceneVersionCompatibleWithAndroid = "4.7.2"


    slf4jVersion = "1.7.30"

    /*      Test     */

    junitVersion = "5.5.2"
    assertJVersion = "3.12.2"
    mockitoVersion = "2.22.0"
    javaUtilsVersion = "1.0.15"
}